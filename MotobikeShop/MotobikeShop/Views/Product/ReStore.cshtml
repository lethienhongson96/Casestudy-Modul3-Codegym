@model List<ReStoreView>

@{
}

@using (Html.BeginForm("Restore", "Product", FormMethod.Post))
{
    <table class="table">
        @for (int i = 0; i < Model.Count; i++)
        {
            @Html.Hidden("ReStoreViews.Index", (@i + 10))

            @Html.Hidden("ReStoreViews[" + (@i + 10) + "].Id",
                   Model[i].Id)
            <tr>
                <td>
                    @Html.TextBox("ReStoreViews[" + (@i + 10) + "].Name",
                    Model[i].Name, new { @readonly = "readonly", @class = "form-control" })
                </td>
                <td>
                    @Html.CheckBox("ReStoreViews[" + (@i + 10) + "].IsRestore", Model[i].IsRestore) Khôi Phục
                </td>
            </tr>
        }
        <tr>
            <td>
                <button type="submit" class="btn btn-success">Lưu Tất Cả Thay đổi</button>
            </td>
        </tr>
    </table>
}